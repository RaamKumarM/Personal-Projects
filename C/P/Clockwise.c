// Clockwise

#include <stdio.h>

int main()
{
    int c,n,s,b; 
    int a[5]={1,2,3,4,5};
    s=sizeof(a)/sizeof(a[0])
    scanf("%d", &n);
    c=a[0];
    for(int i=1 ; i<=s ; i++)
    {
        if ((i+n)>s)            
            a[i+n]=a[i];
        else
        {
            a[i+n-]
        }
    }
}
